name: Deploy portfolio

on:
  push:
    branches:
      - "master"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Build Angular application
        run: npm run build --prod

      - name: Create and switch to the gh-pages branch
        run: |
          git checkout -b gh-pages
          git rm -rf .

      - name: Copy dist folder contents to the gh-pages branch
        run: |
          cp -r dist/* .

      - name: Commit and push changes to gh-pages
        run: |
          git config --local user.email "kaloyangfx@gmail.com"
          git config --local user.name "Kaloyan Stoyanov"
          git add -A
          git commit -m "Deploy to GitHub Pages"
          git push origin gh-pages

      - name: Switch back to the master branch
        run: git checkout master
